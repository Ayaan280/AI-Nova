flask
groq
openai
gunicorn
Pillow
python-dotenv
replit
# Removed stability-sdk from primary requirements to prevent build failure on Render.
# It will be dynamically handled in main.py if the dependency is missing.
